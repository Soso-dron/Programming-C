#include <stdio.h>
#include <math.h>
int main() {
	double a, b, c;
	
	a= 1;
	b = 25;
	c = 13;
	
	double discrit = b * b - 4 * a * c;
	double x1 = (-b + sqrt(discrit))/ (2 * a);
	double x2 = (-b - sqrt(discrit))/ (2 * a);
	
	printf("Results\n");
	printf("Fisrt: %.4f\n", x1);
	printf("Second: %.4f\n", x2);
	
	return 0;
}
